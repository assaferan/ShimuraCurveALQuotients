---
name: handoff-2026-08-27
description: "START HERE (supersedes handoff-2026-08-26-night). Session arc: eis32k (Kubota closed form wired into the pipeline, validation-gated) -> the ELEVENTH BASE 330_1, 4-for-4 -> the N=1 deconfounder (4 tiny new bases) -> and then the SCALE 2 DISSOLVED: it is the tracked isotropic coset, a normalization convention, not arithmetic. Mass-ratio law + all support verdicts survive (scale-invariant); absolute weights are NOT comparable across regimes, so the PREPRINT NEEDS CORRECTING -- that is now the top item. Banked 5385deb. NOTHING running."
metadata: 
  node_type: memory
  type: project
  originSessionId: 6c49569e-411f-4d39-8b37-83e757202459
  modified: 2026-08-26T19:27:35.241Z
---

# HANDOFF 2026-08-27

Supersedes [[handoff-2026-08-26-night]] (its base-11 item is DONE; its
scale-2 and 2^(ω−1) items are RESOLVED — they dissolved).

**NOTHING of ours is running.** Campaign branch m0-theta-campaign at
**5385deb**, fully pushed, 0 unpushed. Main repo tier1-models unchanged
at 32a864e (its working tree has only the usual untracked cruft:
.weyl-snapshot/, qprobe3.m, qprobe4.m, {{pkgetc}}/).

## THE ARC OF THE SESSION (read this order — the later steps overturn the earlier)

**1. eis32k: the closed form became the driver** (46d6849).
Wired [[kubota-rho-closed-form]] into the pipeline as
`vvdata/weyl-campaign/eis32k.m`, replacing the FFT rhov loop. Gated
against the banked 210_1 FFT run: 960/960 RHOV words, resid 1.0087e-42
identical, 153 BETAs to 6e-38. Details + production gotchas in
[[eis32k-closed-form-driver]]. Also banked `genrestrict.py` (1587448),
the gauge-check generator, verified to reproduce the hand-written
restrict210A.m exactly.

**2. THE ELEVENTH BASE 330_1** (prereg cb634d7, verdict 3739fe3).
First run ever at M = 660. resid 1.709e-42.

    E_eis = −2 Gross(165,1) + 3 Gross(165,2)   mod cusp

4-for-4 on its preregistration (support q = D/2 = 165 with no third
term; weights (−2,+3); mass ratio 3/2 from masses 5/12, 5/8 computed in
advance; rank 15 / aliasing 25). Falsifier fired: the competing
largest-prime support {(11,1),(11,30)} EXCLUDED at rank 135 vs 136,
while the winner alone carries E_eis with **aliasing dim 0**. Recorded
in advance: 330 CANNOT separate the two re-split rules (they coincide on
every even D) — it is a replication at fresh primes.

**3. The N=1 deconfounder** (prereg f2d599e, verdict f01cf5c).
Noticed that composite-q and N=1 were PERFECTLY CONFOUNDED across all 11
bases. The missing cell is prime q AND N=1, i.e. D = 2q at
M = 20/28/44/52 — tiny, and only reachable since N=1 support entered the
pipeline. Measured at q = 5,7,11,13: all four give (−2,+3), same as the
composite bases. Also found the support is DEGENERATE at D=2q (both
ramified primes give valid decompositions) and — the durable part —
**the mass law is GAUGE-INVARIANT**: |w2/w1| = mass ratio on BOTH
supports at all four bases.

**4. THE SCALE 2 DISSOLVED** (9c3153a, then 5385deb) — see
[[scale-2-is-forced]] for the full account.
First: the scale was never a free parameter (w1+w2 = 1/2 at every N>1
base, forced by w3 = −1/2 and weight-sum 0; = 1 at every N=1 base), so
scale = 2 identically, killing the "2 vs 2^(ω−1)" question. Then the
cause: the pipeline tracks ρ at ONE isotropic coset and #iso = 2N−1, so
N>1 tracks a NONZERO isotropic coset and N=1 is forced to the ZERO one.
Forcing the zero coset at an N>1 base (`EST:=0`) reproduces the whole
N=1 phenomenology:

| base | tracked coset | fit | sum |
|---|---|---|---|
| 15_2 | nonzero (default) | −1/2 Gr(5,2) + 1 Gr(5,6) − 1/2 Gr(30,1) | 0 |
| 15_2 | **zero** | **−1 Gr(5,2) + 2 Gr(5,6)** | **1** |
| 22_3 | nonzero (default) | −1 Gr(11,3) + 3/2 Gr(11,6) − 1/2 Gr(66,1) | 0 |
| 22_3 | **zero** | **−2 Gr(11,3) + 3 Gr(11,6)** | **1** |

At 22_3 the zero-coset fit is literally (−2,+3) — the N=1 numbers.
**The scale 2 is a normalization convention. There is nothing to derive.**

## WHAT IS NOW TRUE / FALSE (be careful — I refuted two of my own readings)

REFUTED, both mine from earlier the same day:
* "The doubling tracks N=1" (step 3's verdict). N=1 does not cause it;
  it only removes the choice (#iso = 1).
* "The third term is absent at N=1 because (D,1) is indefinite, formal
  mass 0." FALSE — at 15_2 the slot (30,1) is definite (1 class) and
  still vanishes under EST:=0.

SURVIVES UNTOUCHED (all scale-invariant, and now EXPLAINED rather than
lucky):
* The mass-ratio law — the gauge-invariant content of these fits; that
  is why it held 15-for-15 across both regimes AND on both supports.
* Every SUPPORT verdict, incl. the 210_1/330_1 re-split to q = D/2 and
  all rank-exclusions — rescaling weights does not change which columns
  are used.
* All residuals, rank certificates, gauge checks.

## NEXT (rough priority)

0. **PREPRINT CORRECTION — top item, and it is a correctness issue, not
   an update.** The s-law section (tier1-models 44adfce) quotes absolute
   weights without naming the tracked coset, and now spans BOTH regimes
   (bases 10-11 are N=1, the earlier nine are N>1), so those weights are
   not mutually comparable as written. Restate in the convention-free
   mass-ratio form, or name the coset explicitly. Then fold in the
   composite-s arc, the 330_1 scorecard, and the four N=1 bases.
1. **The re-split rule — the genuine open mathematics.** Needs an odd D
   with FOUR primes, minimal D = 1155 (N=1, M = 4620): no EVEN D can
   ever separate "odd part" from "largest definite divisor" (they
   coincide), so 210 and 330 were replications. Two blockers, both in
   eis32k: (i) odd D ⇒ level 4DN and a NON-elementary 2-part, so the
   Jordan-basis code (which errors out unless (Z/2)^3) needs Stromberg
   Def 2.15 extended to Z/4 components; (ii) the YTab preimage/|D[c]|
   scans are O(|D|) and |D| ~ 1e7 there — needs a CRT/per-component
   closed form. **Do (ii) first**: mechanical, and it speeds every base.
2. **w_sq readout / level-channel Zagier component** (carried over from
   [[ternary-theta-recognition]]).
3. [[post-m0-backlog-sweep-plan]] update — still not done.
4. Optional: the bare-S sign's origin (one measured bit per base;
   eis32k measures it by FFT rather than guessing, so this is cosmetic).

## STATE / GOTCHAS

* **CI**: 5 runs from this session were still in_progress at handoff
  (all m0-theta-campaign); everything earlier is green, except a
  tier1-models scorecard run from the morning still showing `queued`.
  Tests take ~1h20m. A real failure would be surprising — this session's
  commits are vvdata scripts/logs/markdown only. Check `gh run list`.
* **The campaign worktree lives in a /tmp scratchpad that purges
  nightly** (…/e91ada1b…/scratchpad/campaign). Branch fully pushed;
  recreate with `git worktree add <path> m0-theta-campaign`. Its
  uncommitted polymake/* artifacts (nmzsolve .err files, 420-scripts)
  are regenerable; the real fixes are committed (b95b07f, b2859fc).
* **eis32k specifics**: `PR:=` overrides precision (use 120 at M≥660);
  `EST:=0` forces the zero coset, `EST:=k` picks isoidx[k]. The
  Jordan-basis search assumes an elementary (Z/2)^3 2-part.
* **ALWAYS run the control.** The c=0-word bug (see
  [[scale-2-is-forced]]) produced a clean-looking EST:=0 result off a
  broken driver; only the control (which failed to reproduce the banked
  |ρ|) exposed it. Comparing |ρ| against a banked run is the cheap
  check.
* For a base with no mono dump, enum32m and genallgross only need the
  BASE line — synthesize it (the character key is universal:
  s1 = s2 = 0 mod 24, parity bit 1 at p=2 only). Used for 330_1 and all
  four N=1 bases.
* **zsh traps**: unquoted `$var` does NOT word-split (use `${=var}` or
  explicit args) — this silently produced `mono10 5_1_synth.log`; and
  `echo ===` fails (quote it).
* Backgrounded `cd` does not persist to later Bash calls — use absolute
  paths or they run in the main repo.
* Magma: `Max` rejects two complex args (take `Abs` first); parse logs
  only after flattening the 80-col wrap.
* Old zombies unchanged: Monday's genusfit15/grossid100 idle Magmas plus
  the foreign core-project trigger.m/jcheck.m Magmas — leave them (~177
  magma processes, none ours).
